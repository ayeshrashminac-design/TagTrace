# Node modules (Skip uploading heavy dependency folders)
node_modules/
backend/node_modules/
frontend/node_modules/

# Environment variables (Protect sensitive database credentials and secrets)
.env
backend/.env

# Executables and local binaries (Skip ngrok and other OS-specific executables)
*.exe